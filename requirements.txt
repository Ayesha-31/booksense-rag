# CPU index for torch is no longer needed if you stay with sentence-transformers CPU wheels;
# keep it only if you still have torch in the file.
# --extra-index-url https://download.pytorch.org/whl/cpu

# 1) Streamlit that is compatible with rich 14
streamlit==1.41.1
rich==14.2.0

# 2) Milvus client + deps
pymilvus==2.4.6
environs==9.5.0
marshmallow==3.21.3

# 3) Your libs
groq>=0.9.0
python-dotenv>=1.0.1
rapidfuzz>=3.9.6
sentence-transformers==2.6.1

# If you’re still using torch via sentence-transformers, add:
torch==2.5.1
transformers>=4.41.1
huggingface-hub>=0.23.0
numpy>=1.26
